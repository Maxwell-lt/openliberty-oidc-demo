<?xml version="1.0" encoding="UTF-8"?>
<server description="new server">

    <!-- Enable features -->
    <featureManager>
        <platform>jakartaee-10.0</platform>
        <platform>microProfile-7.1</platform>
        <feature>mpHealth</feature>
        <feature>jaxrs</feature>
        <feature>jsonb</feature>
        <feature>openidConnectClient-1.0</feature>
        <feature>transportSecurity-1.0</feature>
        <feature>appSecurity</feature>
    </featureManager>

    <!-- This template enables security. To get the full use of all the capabilities, a keystore and user registry are required. -->

    <!-- For the keystore, default keys are generated and stored in a keystore. To provide the keystore password, generate an 
        encoded password using bin/securityUtility encode and add it below in the password attribute of the keyStore element. 
        Then uncomment the keyStore element. -->
    <!--
    <keyStore password=""/> 
    -->

    <!--For a user registry configuration, configure your user registry. For example, configure a basic user registry using the
        basicRegistry element. Specify your own user name below in the name attribute of the user element. For the password,
        generate an encoded password using bin/securityUtility encode and add it in the password attribute of the user element.
        Then uncomment the user element. -->
    <basicRegistry id="basic" realm="BasicRealm">
        <!--
        <user name="yourUserName" password="" />
        -->
    </basicRegistry>

    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="9080"
                  httpsPort="9443" />

    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true"/>

    <webApplication location="liberty-testing-21.war" id="liberty-testing" name="liberty-testing" >
        <application-bnd>
            <security-role name="users">
                <special-subject type="ALL_AUTHENTICATED_USERS"/>
            </security-role>
        </application-bnd>
    </webApplication>

    <!-- Default SSL configuration enables trust for default certificates from the Java runtime -->
    <ssl id="defaultSSLConfig" trustDefaultCerts="true" />

    <variable name="CLIENT_SECRET" defaultValue="test"/>

    <openidConnectClient id="authelia"
                         clientId="bc35915f-0c81-473c-b42d-439b070b4289"
                         clientSecret="${CLIENT_SECRET}"
                         discoveryEndpointUrl="https://auth.maxwell-lt.dev/.well-known/openid-configuration"
                         authFilterRef="api"
                         pkceCodeChallengeMethod="S256"
                         signatureAlgorithm="RS256"
                         userIdentifier="name"
                         uniqueUserIdentifier="sub"
                         scope="openid profile"
                         userInfoEndpointEnabled="true"
                         tokenOrderToFetchCallerClaims="IDToken"
    >
        <tokenParameter name="scope" value="openid profile"/>
    </openidConnectClient>

    <authFilter id="api">
        <requestUrl urlPattern="/api" matchType="contains"/>
    </authFilter>
</server>
